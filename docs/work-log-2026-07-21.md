# Work Log - 2026-07-21

## Session metadata
- Date: 2026-07-21
- Workspace: `K-youtube`
- Branch: `dev/kitchen-v1`
- Tester/Operator: Copilot assisted run
- Primary target: Android real device + local Supabase

## Completed today

### 1. Local Supabase recovery and smoke debugging
- Resolved Docker Desktop prerequisite issue and restarted local Supabase services.
- Recovered Android local connectivity by reapplying `adb reverse tcp:54321 tcp:54321`.
- Verified local API reachability for:
  - `/rest/v1`
  - `/functions/v1/recipe_api`
- Identified that kitchen summary failures were caused by missing local migrations after reset.
- Applied migrations and confirmed kitchen tables were created:
  - `kitchen_ingredients`
  - `kitchen_shopping_lists`
  - `kitchen_shopping_items`
  - `kitchen_cook_sessions`

### 2. Login debugging and usability improvement
- Diagnosed local login failure path.
- Created/verified a local test user for Supabase auth.
- Improved login error messaging in the app:
  - invalid credentials -> Korean guidance
  - rate limit -> retry guidance
  - unconfirmed email -> confirmation guidance
  - socket/connection failure -> `adb reverse` hint

### 3. AI recipe assistant upgrade
- Replaced placeholder `ai_recipe_assistant` response with a structured rule-based assistant.
- Added response fields:
  - `summary`
  - `tips`
  - `cautions`
  - `engine`
- Switched Flutter `AiService` from local substring fallback to real Edge Function invocation with fallback handling.
- Added AI summary generation UI to recipe detail page.
- Exposed AI summary output in the UI.

### 4. AI observability and cost guardrails
- Extended `ai_usage_logs` with additional guardrail and telemetry fields:
  - `latency_ms`
  - `request_chars`
  - `response_chars`
  - `guardrail_stage`
  - `meta`
- Added monthly guardrail aggregation view:
  - `ai_usage_monthly_guardrails`
- Implemented per-request AI usage logging in `ai_recipe_assistant`.
- Fixed FK failure on `ai_usage_logs.user_id` by ensuring `profiles` exists before logging.
- Verified logs are stored for authenticated users and visible in the monthly aggregation view.

### 5. YouTube metadata worker foundation
- Added migration for `recipe_youtube_metadata` table and RLS policies.
- Implemented new Edge Function:
  - `youtube_metadata_worker`
- Supported actions:
  - `sync_one`
  - `sync_pending`
- Added oEmbed-based YouTube metadata fetch and upsert behavior.
- Fixed duplicate-key failure by using `on_conflict=recipe_creator_id`.
- Verified:
  - `sync_one` success
  - `sync_pending` success
  - metadata persisted with parsed YouTube video id

### 6. Social login foundation (Google / Kakao)
- Added Supabase PKCE auth initialization in Flutter bootstrap.
- Added Android deep link callback:
  - `io.supabase.kyoutube://login-callback`
- Added iOS URL scheme callback for the same scheme.
- Added Google login button in login page.
- Added Kakao login button in login page.
- Added automatic login-page close behavior after auth state change.
- Added local Supabase auth config placeholders for:
  - Google OAuth
  - Kakao OAuth

## Files changed
- `lib/app.dart`
- `lib/features/auth/presentation/login_page.dart`
- `lib/features/ai/application/ai_service.dart`
- `lib/features/recipes/presentation/recipe_detail_page.dart`
- `supabase/functions/ai_recipe_assistant/index.ts`
- `supabase/functions/youtube_metadata_worker/index.ts`
- `supabase/functions/.env`
- `supabase/config.toml`
- `android/app/src/main/AndroidManifest.xml`
- `ios/Runner/Info.plist`
- `supabase/migrations/0008_ai_usage_guardrails.sql`
- `supabase/migrations/0009_youtube_metadata_foundation.sql`

## Validation performed
- `flutter analyze`
- `flutter analyze lib/app.dart lib/features/auth/presentation/login_page.dart`
- `flutter analyze lib/features/ai/application/ai_service.dart lib/features/recipes/presentation/recipe_detail_page.dart`
- Local HTTP verification for Supabase REST and Edge Functions
- DB verification for:
  - kitchen tables
  - AI guardrail columns
  - YouTube metadata table
  - AI monthly guardrail view
- Runtime verification for:
  - `ai_recipe_assistant`
  - AI usage logging
  - `youtube_metadata_worker sync_one`
  - `youtube_metadata_worker sync_pending`

## Current blockers / risks
- Local Supabase full stack remains unstable on this Windows machine when `studio`, `analytics`, `realtime`, or `edge_runtime` health checks churn.
- Stable workaround used today:
  - `supabase start -x studio,analytics,vector,realtime --ignore-health-check`
- Google/Kakao social login is implemented in the app, but real end-to-end login still requires actual provider credentials in Supabase auth config.
- Current login screen still shows credential failure for email/password if the local test user was reset by another local DB restart.

### 7. Account management UI
- Added `profileProvider` (FutureProvider) to `auth_providers.dart` to fetch `profiles` row.
- Added `supabaseClientProvider` helper provider.
- Created `lib/features/auth/presentation/profile_page.dart`:
  - Avatar (이니셜 기반), display_name, email 표시
  - 연결된 소셜 프로바이더 목록 (identities 기반)
  - 역할(role) 표시
  - 로그아웃 (확인 다이얼로그 → `context.go('/')`)
  - 계정 삭제 (확인 다이얼로그 → `delete_account` RPC 호출)
- 라우터에 `/profile` 경로 등록.
- 홈 AppBar 로그아웃 버튼을 프로필 버튼(`Icons.account_circle_outlined`)으로 교체.
  - 미로그인 시 `/login` 이동, 로그인 시 `/profile` 이동.
- 마이그레이션 `0010_delete_account_rpc.sql` 추가: `delete_account()` RPC 함수 (authenticated only).
- `supabase db reset` 성공 확인.

## Recommended next start point
1. Populate real Google OAuth client id/secret and Kakao client id/secret in local or hosted Supabase auth configuration.
2. Re-run the app with the current local stack workaround and verify real Google/Kakao callback flow on device.
3. (완료) Account management UI — `/profile` 페이지 구현 완료.
4. 프로필 편집 기능 추가 (display_name 수정) — 선택적.

## End-of-day summary
- Core local backend, kitchen flow, AI summary flow, AI telemetry, and YouTube metadata worker foundations are in place.
- Social login foundation is coded and statically validated.
- Remaining work is now mostly provider credential setup, end-to-end OAuth verification, and user account management UI completion.